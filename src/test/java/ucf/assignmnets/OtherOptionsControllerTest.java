package ucf.assignmnets;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class OtherOptionsControllerTest {

    @Test
    void displayComplete() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the DissplayComplete Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void displayIncomplete() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the displayIncomplete Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void displayToDoListItems() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the displayToDoListItems Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void editDateOfItem() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the editDateOfItem Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void editDescriptionOfItem() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the editDescriptionOfItem Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void loadToDoList() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the loadToDoList Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }
}