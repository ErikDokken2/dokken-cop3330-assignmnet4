package ucf.assignmnets;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class HomePageControllerTest {

    @Test
    void addListButClicked() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the Add List Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void revListButClicked() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the Remove List Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void editListNameClicked() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the EditList Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void removIteamClicked() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the RemoveItem Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void addIteamClicked() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the addIteam Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }

    @Test
    void saveOptionClicked() {
        //make a new Homepage controller
        //initialize resultDisplay to some non-blank value
        //call the Save Button
        //Get the new value in the ResultDisplay

        //Compare the old value with the new value
    }
}